# Whacked — front (Vite + React)

- `npm install` → `npm run dev` (porta 8080; `/api` faz proxy para `http://127.0.0.1:3001`).
- API Node na pasta `../server`; variáveis em `../.env` (ver `../.env.example`).
- Réplicas só de marca: `../cornfield_front` (porta 8081), `../gearcraft_front` (porta 8082), mesma API.
